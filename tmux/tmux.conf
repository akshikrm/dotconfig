# terminal colors
set -g default-terminal "screen-256color"
set-option -ga terminal-overrides ",xterm-256color:Tc"
#changing default prefix
set -g prefix C-Space 
unbind C-b
bind-key C-Space send-prefix

# reload the tmux config
unbind r
bind r source-file $XDG_CONFIG_HOME/tmux/tmux.conf

# enable mouse mode
set -g mouse on


# Start counting pane and window number at 1
set -g base-index 1
setw -g pane-base-index 1

set-option -g status-style fg=#bababa,bg=#101010
set-option -g status-right "#[bg=#e6e6e6,fg=#1a1a1a] %d %b %y #[bg=#1a1a1a,fg=#e6e6e6] %H:%M "


bind -r j resize-pane -D 5
bind -r k resize-pane -U 5
bind -r l resize-pane -R 5
bind -r h resize-pane -L 5
bind -r m resize-pane -Z

set -g @plugin 'tmux-plugins/tpm'
# set -g @plugin 'Nybkox/tmux-kanagawa'
set -g @plugin 'christoomey/vim-tmux-navigator' 
set -g @plugin 'tmux-plugins/tmux-resurrect' 
set -g @plugin 'tmux-plugins/tmux-continuum'

set -g @resurrect-capture-pane-contents 'on' 
set -g @continuum-restore 'on'
# set -g @kanagawa-theme 'wave'
# set -g @kanagawa-plugins "time cpu-usage ram-usage git"
# set -g @kanagawa-time-format "%H:%M %b %d"
# set -g @kanagawa-hg-no-repo-message ""
#
run '$XDG_CONFIG_HOME/tmux/plugins/tpm/tpm'
